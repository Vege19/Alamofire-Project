# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

source 'https://github.com/CocoaPods/Specs.git' 
platform :ios, '10.0'

target 'AlamofireProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AlamofireProject

  # alamofire
  pod 'Alamofire', '~> 4.5'
  #Siwfty son
  pod 'SwiftyJSON', '~> 4.0'
  #SDWImage
  pod 'SDWebImage', '~> 5.0'

  target 'AlamofireProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AlamofireProjectUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
